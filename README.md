<h1> steps to setup</h1>
<br>
1.create a <code>.env</code> file define TWITTER_API_KEY= <-api key->
2.<code>npm run start</code> on terminal to start the server
3.Open the html webpage and get latest tweets of any user!
